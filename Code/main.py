#TO be the main code
import machine
import network
import Sensor
import CommInternet
import ujson as json
import time
import network

while True:
    def DisableAp()
    def ConnectWifi()
    if not sta_if.isconnected():
        print("not connected")
        exit()
    def SendJson(data)
    time.sleep(1)
    def subscribe(server="localhost")
    
    
    
    time.sleep(300)
    
    
