#TO be the main code
import machine
import network
import Sensor
import CommInternet
import ujson as json
import time
import network


def DisableAp()
def ConnectWifi()
if not sta_if.isconnected():
    print("not connected")
    exit()
def SendJson(data)
